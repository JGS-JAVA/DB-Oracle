/*
join
둘 이상 표에서 데이터 결합
INNER JOIN = 공통값 있는 행만 조회

LEFT(OUTER) JOIN = 왼쪽 표의 모든행 가져오기, 오른쪽 표에 값 없으면 NULL로 채움
RIGHT(OUTER) JOIN = 오른쪽 표의 모든행 가져오기, 왼쪽 표에 값 없으면 NULL로 채움
FULL JOIN (MYSQL에서 지원X, UNION 사용) = LFET JOIN 과 RIGHT JOIN 을 합친결과










*/